- A block in a flow said what it hands on by asking what its own contract produces, and every one-to-one block that was not a combine or a split was described as making an "image result" — so Organize PDF, in a chain that has never held an image, announced one. A block now says what it produces in the chain it is actually in, which is the only place the answer exists for a Gizlet that can hand on either of two things.
